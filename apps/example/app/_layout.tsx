/**
 * Legacy Expo Router layout file.
 *
 * The app now uses React Navigation from `App.tsx`; this component is kept as a
 * no-op so existing imports in the `app/` directory remain valid.
 */
export default function RootLayout() {
  return null;
}
