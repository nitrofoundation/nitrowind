# Next.js example

Run `yarn workspace nitrowind-nextjs-example dev`.

`app/page.tsx` is a Server Component and uses real HTML elements. The only
Nitrowind import is CSS: `@nitrofoundation/nitrowind/next.css`. Do not import
the `@nitrofoundation/nitrowind` JavaScript entry from a Next server component.
