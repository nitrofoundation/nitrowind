# Vite example

Run `yarn workspace nitrowind-vite-example dev`.

The example uses Vite's Tailwind plugin and real DOM elements. Nitrowind is a
CSS-only import: `@nitrofoundation/nitrowind/vite.css`.
